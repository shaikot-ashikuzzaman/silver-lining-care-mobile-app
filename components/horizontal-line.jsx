import React from "react";
import { View } from "react-native";

const HorizontalLine = ({ style }) => {
    return <View className="h-px bg-neutral-200" style={style} />;
};

export default HorizontalLine;
